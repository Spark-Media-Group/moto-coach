{
  "name": "moto-coach-website",
  "version": "1.0.0",
  "description": "Moto Coach Professional Motocross Training Website",
  "scripts": {
    "dev": "echo 'Use: vercel dev' && exit 1",
    "build": "echo 'Static site - no build needed'",
    "start": "echo 'Use: vercel dev' && exit 1"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1",
    "googleapis": "^128.0.0",
    "resend": "^3.2.0",
    "stripe": "^19.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
